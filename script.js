//your JS code here. If required.
function showURLLength() {
    const url = location.href;   // get current URL
    const len = url.length;      // length of URL
    alert("The length of the URL is: " + len);
}
